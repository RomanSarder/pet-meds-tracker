// Owned by the Supplies wave.
import type { Formatters } from "../formatters";

export interface SuppliesMessages {}

export const enSupplies = (_f: Formatters): SuppliesMessages => ({});

export const ukSupplies = (_f: Formatters): SuppliesMessages => ({});
