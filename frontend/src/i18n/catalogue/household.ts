// Owned by the Household wave.
import type { Formatters } from "../formatters";

export interface HouseholdMessages {}

export const enHousehold = (_f: Formatters): HouseholdMessages => ({});

export const ukHousehold = (_f: Formatters): HouseholdMessages => ({});
