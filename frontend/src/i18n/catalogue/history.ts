// Owned by the History wave.
import type { Formatters } from "../formatters";

export interface HistoryMessages {}

export const enHistory = (_f: Formatters): HistoryMessages => ({});

export const ukHistory = (_f: Formatters): HistoryMessages => ({});
