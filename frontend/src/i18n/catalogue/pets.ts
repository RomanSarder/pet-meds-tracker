// Owned by the Pets wave.
import type { Formatters } from "../formatters";

export interface PetsMessages {}

export const enPets = (_f: Formatters): PetsMessages => ({});

export const ukPets = (_f: Formatters): PetsMessages => ({});
