// Owned by the Today wave.
import type { Formatters } from "../formatters";

export interface TodayMessages {}

export const enToday = (_f: Formatters): TodayMessages => ({});

export const ukToday = (_f: Formatters): TodayMessages => ({});
