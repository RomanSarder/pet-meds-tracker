// Owned by the Schedule wave.
import type { Formatters } from "../formatters";

export interface ScheduleMessages {}

export const enSchedule = (_f: Formatters): ScheduleMessages => ({});

export const ukSchedule = (_f: Formatters): ScheduleMessages => ({});
